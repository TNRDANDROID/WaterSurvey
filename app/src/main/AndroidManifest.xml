<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nic.watersurveyform">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name="com.nic.watersurveyform.application.NICApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name="com.nic.watersurveyform.activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name="com.nic.watersurveyform.activity.LoginScreen"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateUnchanged|adjustPan" />
        <activity
            android:name="com.nic.watersurveyform.activity.HomePage"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateUnchanged|adjustPan" />
        <activity
            android:name="com.nic.watersurveyform.activity.WaterConnection"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateUnchanged|adjustPan" />

    </application>

</manifest>